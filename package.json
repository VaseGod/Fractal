{
  "name": "fractal-infrastructure",
  "version": "1.0.0",
  "private": true,
  "description": "Fractal — Self-improving agentic infrastructure",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev:jobs": "cd jobs && npx trigger.dev@latest dev",
    "build:jobs": "cd jobs && npx tsc",
    "lint": "cd jobs && npx tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@trigger.dev/sdk": "3.3.14",
    "@browserbasehq/sdk": "2.2.0",
    "puppeteer-core": "23.11.1",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "typescript": "5.7.3",
    "@types/node": "22.12.0"
  }
}
